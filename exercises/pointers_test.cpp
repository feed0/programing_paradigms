#include <iostream>
#include <assert.h>

#include "pointers/pointers.h"

using namespace std;

/*POINTERS EXERCISES
    - 1 Write the strcpy(s, t) function that receives two strings and copies `t` onto `s`. Asume that the sizes are equivalent. 
    
    - 2 Escreva a função strcmp(s, t) que recebe duas strings como parâmetro e compara s e t, retornando um valor negativo, zero ou positivo se s é lexicograficamente menor, igual ou maior que †, respectivamente
    - 3 Escreva a função strcat(s, f) que recebe como parâmetro duas strings e concatena t em s
    - 4 Escreva a função strlen(s) que recebe como parâmetro a string s e retorna seu comprimento
*/


int main() {

    // TEST strcpy

    // string s = "Hello", t = "World";
    // strcpy(&s[0], &t[0]);
    // assert(s == t);
    // assert(t == "World" || t != s); // error

    return 0;
}
